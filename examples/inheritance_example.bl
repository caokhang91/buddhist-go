// Inheritance and Constructor Example - Buddhist Lang

println("=== Inheritance Example ===");

// Base class
class Animal {
    place name = null;
    
    fn init(n) {
        this.name = n;
    }
    
    fn speak() {
        return this.name + " makes a sound";
    }
}

// Derived class
class Dog extends Animal {
    place breed = null;
    
    fn init(n, b) {
        this.name = n;
        this.breed = b;
    }
    
    fn speak() {
        return this.name + " barks!";
    }
    
    fn getInfo() {
        return this.name + " is a " + this.breed;
    }
}

// Create instances
place animal = Animal("Generic Animal");
println(animal.speak());

place dog = Dog("Buddy", "Golden Retriever");
println(dog.speak());
println(dog.getInfo());

println("\n=== Demo Complete ===");
